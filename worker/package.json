{
  "name": "lastlight-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "test": "node --test worker.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}

